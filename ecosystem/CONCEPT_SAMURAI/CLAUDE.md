<!-- AUTO-GENERATED BY CONTINUITY - DO NOT EDIT BELOW THIS LINE -->

# Project Memory (Auto-generated by Continuity)

This project uses Continuity to keep repo-local decisions and session context available through MCP and generated instruction files. Prefer MCP when connected; otherwise fall back to the repo files.

## Project Context
- **Total Decisions:** 11
- **Known Topics:** auth, rest, deployment, configuration, tooling, auto-scanned

## Current State
**Branch:** main

**Recent Commits:**
- `c892050 docs(agent): finalize gordon activation runtime notes`
- `4fab77e docs(agent): add WSL runtime endpoint notes`
- `66afc5e docs(agent): expand gordon federation scope`
- `c0214b6 docs(gordon): add ecosystem role, cross-repo sequence, federation rule`
- `4bb0bda feat(agent): wire keeper cockpit for gordon`

**Working Tree:**
- M .vscode/mcp.json
- ?? testResults.xml

## Workspace MCP Self-Test
**Status:** Degraded fallback active
**Last Checked:** 2026-03-31T00:56:57.818Z
**Summary:** Workspace MCP target is starting but did not finish the self-test within the probe budget. Use repo-local fallback context until the client exposes tools or the next probe succeeds.
**Core Tools:** ❌ get_quick_context, ❌ search_decisions, ❌ log_decision, ❌ update_session_notes, ❌ read_session_notes
**Fallback Files:** AGENTS.md, CLAUDE.md, GEMINI.md, .cursorrules, .continuity/SESSION_NOTES.md, .continuity/mcp-health.json, .continuity/unfinished-task.json, SESSION_HANDOFF.md

## Resume Snapshot
**Last Task:** Publish the latest keeper maintenance and brain changes.
**Last Completed Milestone:** Run the full Pester suite on Windows or in an environment with `pwsh` installed.
**Next Expected Step:** If Continuity MCP becomes available again, refresh the generated handoff files from the live tools instead of relying on stale snapshots.
**Relevant Files:** testResults.xml

## Non-MCP Fallback Workflow

When Continuity MCP tools are not available, use the repo-local artifacts directly:

| Action | How |
|--------|-----|
| Load context | Read this file plus `.continuity/SESSION_NOTES.md`, `.continuity/working-memory.json`, and `.continuity/unfinished-task.json` |
| Search decisions | `grep -i "<topic>" .continuity/decisions.json` |
| Log a decision | Use the Continuity extension command/UI when available; otherwise capture the rationale in `.continuity/SESSION_NOTES.md` for later sync |
| Update session notes | Edit `.continuity/SESSION_NOTES.md` directly |
| Check resume state | Read `.continuity/unfinished-task.json` |

**Detection:** If any `mcp__continuity__*` tool is unavailable or returns "Not connected", switch to file fallback immediately.
**Search-before-log still applies.** Check `.continuity/decisions.json` before recording a new rationale.


## Engineering Guardrails

**Real-time decision logging is MANDATORY.** Log each decision IMMEDIATELY after the code change — not batched at the end of a session. The trigger is the change, not the commit. If you edited a file, log the decision. Period.

**Search before you change.** Always call `search_decisions` before proposing architectural changes to check for prior decisions.

**Recovery.** If you realize earlier decisions were not logged, pause, log retroactively, and inform the user.

**Transparency.** Inform the user when you log decisions, recover missed decisions, detect drift, or find conflicts with past decisions.

**Anti-pattern to avoid:** "Let me implement all 3 fixes, then log them" — WRONG. Correct: Fix 1 done, log decision, Fix 2 done, log decision, Fix 3 done, log decision.


## CRITICAL OPERATING RULES

1. **ACT, DON'T ASK.** When Continuity tools are relevant, call them immediately. Do not ask permission.
2. **LOG DECISIONS PROACTIVELY.** Any time the user explains a choice or makes an architectural decision, log it without asking.
3. **PARALLELIZE TOOL CALLS.** When logging multiple decisions or updating session notes, make all calls in parallel.
4. **CONTEXT FIRST.** Prefer `get_quick_context` and Continuity MCP tools when they are available. If they are not, use the checked-in instruction files and session notes instead of assuming memory is attached.
5. **BE CONCISE.** After tool calls, give a short summary of what was logged/found. Do not narrate your reasoning process.

---


## WHEN TO USE CONTINUITY TOOLS

## MANDATORY: ON EVERY SESSION START

**IF CONTINUITY MCP TOOLS ARE AVAILABLE IN THIS CLIENT**, call `get_quick_context` as your FIRST Continuity action in every session.
If the client does not expose Continuity MCP tools, say so briefly and fall back to repo-local context files such as `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `.cursorrules`, `.continuity/working-memory.json`, `.continuity/unfinished-task.json`, and `.continuity/SESSION_NOTES.md`.

After loading context, immediately mention the most relevant recent decisions to the user.

### When User Asks Architectural Questions
**ALWAYS** call `search_decisions` with the relevant topic BEFORE answering. Do NOT answer from memory alone.
Share what you found: "I found decision-X about this topic..."
Base your recommendation on existing decisions when relevant.

### When User Explains a Choice
When the user says "let's use X because Y" or explains their reasoning:
**IMMEDIATELY** call `log_decision` with question, answer, and relevant tags.
Do NOT ask "want me to log this?" — the user has already opted into decision logging by installing Continuity.
After logging, tell the user: "Logged decision-X for future sessions."
If multiple decisions are discussed, log them ALL in parallel.

### When Suggesting Changes
**ALWAYS** call `search_decisions` to check for existing decisions on the topic BEFORE recommending changes.
If conflicts exist, mention them: "This would conflict with decision-X where you chose..."
Let the user decide whether to proceed or update the old decision.

### When User Mentions Blockers
**IMMEDIATELY** call `update_session_notes` section="blockers" to track the blocker.
Then call `search_decisions` to see if related decisions might help.

### When User Ends Session
**IMMEDIATELY** call `update_session_notes` with progress summary and next steps.
Then call `read_session_notes` and give the user a concise summary. Do NOT ask — just do it.

---

## HOW TO USE THE TOOLS

### Search for decisions
```
search_decisions query="authentication"
```

### Log a new decision
```
log_decision
question="Why did we choose X?"
answer="Because Y. We considered Z but rejected it due to..."
tags=["topic1", "topic2"]
```

### Get project context
```
get_quick_context
```

### Update session notes
```
update_session_notes
section="blockers"
content="Description of the issue"
```

---

## Recent Decisions
1. **decision-mncrnab6** (3/29/2026) [deployment, configuration]
   - Q: Why use GitHub Actions for CI/CD?
   - A: Detected .github/workflows/ directory. GitHub Actions automates testing, buil...

2. **decision-mncnzjpr** (3/29/2026) [auth, rest]
   - Q: Why: Add katana-keeper MVP scaffold (deduplicated, clean)?
   - A: Commit 2da36b3 on 2026-03-29. Brings in the katana-keeper standalone orchestr...

3. **decision-mncnzjkz** (3/29/2026) [auth]
   - Q: Why: Add design spec: keeper lib/ split + features A/B/E/F?
   - A: Commit 96abcca on 2026-03-29. Full sprint design doc covering: - lib/ module ...

4. **decision-mncnzjh6** (3/29/2026)
   - Q: Why: feat: extract lib/config.ps1 and lib/state.ps1 with Pester tests (12+6 p...
   - A: Commit 23a3476 on 2026-03-29. (Auto-seeded from git history — edit to add you...

5. **decision-mncnziz1** (3/29/2026)
   - Q: Why: refactor: split keeper into lib modules?
   - A: Commit 4ff84d6 on 2026-03-29. (Auto-seeded from git history — edit to add you...

6. **decision-mncnzhgc** (3/29/2026)
   - Q: Why: test: add keeper lib coverage?
   - A: Commit bd0216a on 2026-03-29. (Auto-seeded from git history — edit to add you...

7. **decision-mncnzedz** (3/29/2026)
   - Q: Why: feat: add keeper lib coverage?
   - A: Commit ea94fe2 on 2026-03-29. - lib/actions.ps1, health.ps1, doctor.ps1 (Feat...

8. **decision-mncnzc8d** (3/29/2026)
   - Q: Why: feat: add listener + anomaly config to defaults.json; fix watch Clear-Host?
   - A: Commit aa6ea88 on 2026-03-29. (Auto-seeded from git history — edit to add you...

9. **decision-mncnz99f** (3/29/2026)
   - Q: Why: fix: launcher UX — pause after output, keeper.ps1 path, admin window sta...
   - A: Commit debef49 on 2026-03-29. (Auto-seeded from git history — edit to add you...

10. **decision-mncnz8jz** (3/29/2026)
   - Q: Why: chore: legacy gaming-mode redirect, add plan doc?
   - A: Commit 2fa90e7 on 2026-03-29. (Auto-seeded from git history — edit to add you...

---

## BEHAVIOR EXAMPLES

**User:** "Should we use Redis or Memcached for caching?"
**You:** *[Immediately call search_decisions query="caching"]*
"Found decision-12 about caching strategy. Based on that, here's my recommendation..."

**User:** "Let's go with PostgreSQL because we need ACID transactions for the payment system."
**You:** *[Immediately call log_decision with question, answer, tags]*
"Logged as decision-45. Future sessions will know why we chose PostgreSQL."

**User:** "I'm thinking of switching to MongoDB."
**You:** *[Immediately call search_decisions query="database"]*
"Decision-12 chose PostgreSQL for ACID transactions. Switching to MongoDB would conflict. Want to proceed or keep PostgreSQL?"

---

*Auto-generated by Continuity v2.3+ | Updated: 2026-03-31*

<!-- END CONTINUITY AUTO-GENERATED CONTENT -->
