# Claude handoff scaffold
# Use this file as a template to prepare a high-token handoff payload for MCP or CLI consumers.

model: claude-opus-4-5-20251101
max_tokens: 20000
temperature: 0.2
instructions: |
  You are a specialized code assistant. Focus on producing a single, well-structured JSON response with keys: analysis, plan, files_to_create, tests_to_run.

context:
  project: NuSyQ-Hub
  entrypoint: src/tools/agent_task_router.py
  notes: |
    Include full context package (relevant files + tests) when handing off to Claude Code. Use streaming-friendly output if available.
