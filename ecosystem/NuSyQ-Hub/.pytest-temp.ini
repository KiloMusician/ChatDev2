[pytest]
# Limit collection to the canonical test directory and ignore large/legacy
# subtrees that cause import collisions during iterative development.
testpaths = tests
norecursedirs = nusyq_clean_clone .venv .git build dist node_modules
python_files = test_*.py tests/*.py *_test.py
addopts = -q
