# Enhanced System TODO & Quest Log

This evolving log collects optimization ideas, tasks, and placeholders for future development.

## Completed Workflow Optimizations
- [x] Draft comprehensive order-of-operations guide linking checklists, quest log, and ZETA tracker (see `docs/ORDER_OF_OPERATIONS_GUIDE.md`).
- [x] Introduce auto-sync between `src/Rosetta_Quest_System/quest_log.jsonl` and `docs/Checklists/PROJECT_STATUS_CHECKLIST.md` (see `scripts/checklist_to_quests.py`).
- [x] Create context-aware prompts for Copilot/ChatDev interactions (see `reports/copilot_prompt_matrix.md`).
- [x] Integrate `python src/diagnostics/system_health_assessor.py` into routine development sessions via `scripts/placeholder_playbook.py`.
- [x] Design plugin registry for seamless feature expansions (see `scripts/placeholder_playbook.py` and `data/plugin_registry.json`).

## Active Priorities (Project Status Checklist)
- [ ] Remove bloat/obsolete files — consolidate or archive redundant reports/scripts before adding new tooling.
- [ ] Optimize directory structure — confirm root clutter is minimized and aligns with the layout in `README.md`.
- [ ] Ensure all modules have docstrings and type hints (in progress) — sweep remaining gaps and align with mypy/ruff expectations.
- [ ] Integrate/extend Copilot and consciousness bridge features (in progress) — reconcile `.github/copilot-instructions.md` with runtime behavior.
- [ ] Performance monitoring and optimization scripts — extend existing monitoring hooks instead of creating new tools (Three Before New).
- [ ] Regularly update `CHANGELOG.md` — summarize recent automation and orchestration changes.
- [ ] Document order-of-operations for Copilot ↔ ChatDev workflow — reconcile missing `docs/NEXT_STEPS_FOR_COPILOT_AND_CLAUDE.md` reference with `NEXT_STEPS_EXECUTION_PLAN.md`.
- [ ] Automate updates to `config/ZETA_PROGRESS_TRACKER.json` — prefer augmenting existing sync scripts before introducing new ones.
- [ ] Develop hint engine to suggest next actions during development — reuse quest/log data for recommendations.

## Orchestration & Signal Health
- [x] Enhance system snapshots with deltas (files changed, errors reduced, agents used) via `scripts/snapshot_delta_report.py` (`reports/snapshot_delta_report.md`).
- [ ] Compare instruction doctrine vs runtime behavior; flag violations and update guidance.
- [ ] Capture emergent behavior from commits/session logs and promote to quests.
- [x] Triage Rosetta quest log duplicates (multiple autogenerated `bug_fixes` questlines with placeholder ValueError entries); consolidated by cancelling 62 placeholder quests on 2026-01-24.

## Creative Enhancements
- [ ] Prototype hint engine to suggest next actions based on repository state (ties to Project Status Checklist).
- [ ] Build automated test coverage explorer for quick gap analysis.
- [ ] Explore procedural quest generation for the Rosetta Quest System.
- [ ] Research multi-agent memory consolidation strategies.
- [ ] Evaluate performance of optimization scripts.
