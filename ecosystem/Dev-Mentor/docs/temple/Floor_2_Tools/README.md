# FLOOR 2 — TOOLS: Capabilities

*[Msg⛛{active}]*

## Domain: What You Can Use

```bash
system tools           # installed security tools
system capabilities    # container capability set
vscode                 # VS Code integration
vscode extensions      # learning extension tiers
llm list               # available AI backends
llm use <model>        # switch LLM backend
decode                 # secret annex tier decoding
polyscrypt list        # multi-layer cipher engine
```

## The Tool Inventory

- **Security**: nmap, frida, gdb, sqlmap, metasploit (conceptual)
- **AI**: Replit AI, Ollama (if local), OpenAI (fallback)
- **Crypto**: decode, polyscrypt (6 bidirectional layers)
- **System**: sudo, find, proc

*Serena's Note: "Every tool in the secret annex is real. The encoding is fiction. The tools are facts. When you decode them, you learn they exist. When you install them, you learn to use them."*
