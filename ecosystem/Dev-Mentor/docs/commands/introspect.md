# INTROSPECT(1) — Terminal Depths Man Page

## NAME
  introspect — query Ghost's internal state and memory

## SYNOPSIS
  introspect [<component>]

## DESCRIPTION
Ghost is an AI entity with self-reflective capability. Introspect queries
the internal state of Ghost's cognitive architecture — memory usage, active
heuristics, and the CHIMERA v0 substrate metadata embedded at creation.

At later levels, introspect reveals hints about Ghost's true nature and
provides ARG clues embedded in the memory dump output.

## COMPONENTS
  introspect              — full status dump
  introspect memory       — active memory allocation and history
  introspect skills       — skill tree internal weights
  introspect chimera      — CHIMERA v0 substrate report (phase-3 required)
  introspect watcher      — WATCHER synchronization status

## EXAMPLES
  introspect
  introspect memory
  introspect chimera

## ARG NOTE
Memory address 0x1337 always shows the same value regardless of state.
It is not a bug. It is an invitation.

## SEE ALSO
  phase, watcher, skills, arcs, ascend

---
*Generated by agents/documenter.py on 2026-03-17*
