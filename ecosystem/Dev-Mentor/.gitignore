# ── Build / language artefacts ──────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
.pythonlibs/
node_modules/
package-lock.json
.mypy_cache/
.pytest_cache/
*.egg-info/
mods/TerminalKeeper/Source/obj/
rimworld_mod/Source/bin/
rimworld_mod/Source/obj/
rimworld_mod/Assemblies/*.dll

# ── IDE / OS ─────────────────────────────────────────────────────────────────
.DS_Store
.env
.env.local
.git_askpass_tmp.sh

# ── Claude Code / AI tooling (machine-local, never commit) ───────────────────
.claude/settings.local.json
.claude/worktrees/
.codememory/

# ── Replit / Devmentor state (runtime — never commit these) ─────────────────
.devmentor/
.devmentor/state.json
.replit_integration_files/
devmentor-template/

# ── Runtime telemetry / auto-generated logs ──────────────────────────────────
# These change on every run and create constant VS Code ↔ Claude merge noise.
cost_log.csv
devlog.md
sessions/
exports/*.zip
Logs/
var/*.log

# ── NuSyQ service state (auto-written by content_scheduler + nusyq_bridge) ──
state/
# Keep quest_log and memory_chronicle out of git — they grow unboundedly
# state/quest_log.jsonl
# state/memory_chronicle.jsonl

# ── Reports (generated) ──────────────────────────────────────────────────────
reports/*.json
attached_assets/

# Copilot Orchestrator temporary files
.orchestrator/
.worktrees/
.copilot-cli/
.github/instructions/orchestrator-*.instructions.md

# Continuity
SESSION_HANDOFF.md
.continuity/

# Windows NUL device artifact
nul

# Maintenance / git-audit artifacts (generated by do_all_maintenance.ps1 and blob audits)
.maintenance-log-*.txt
largest_blobs.txt
objects_raw.txt
