{
  "name": "test_runner",
  "version": "1.0.0",
  "description": "Run pytest on a file or directory, report results, optionally generate missing tests.",
  "entry_point": "plugin.py",
  "category": "development",
  "enabled": true,
  "config_schema": {
    "target": "path to test file or directory (default: tests/)",
    "generate": "true|false — generate missing tests with LLM"
  }
}
