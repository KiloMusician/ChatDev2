<?xml version="1.0" encoding="utf-8"?>
<!--
  Optional Harmony XML patches applied only when RimAPI is loaded.
  Exposes additional colonist fields to Terminal Depths if RimAPI is present.
-->
<Patch>
  <!-- Nothing required yet — C# Harmony patches handle everything at compile time.
       This file is reserved for future XPath patches that depend on RimAPI defs. -->
</Patch>
