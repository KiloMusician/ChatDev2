
This file lists the versions of the packages that are known to work with your code. You can adjust the version numbers if needed, but it's generally a good practice to pin specific versions to ensure consistency and avoid potential issues due to breaking changes in newer versions.

To install these dependencies, you can use pip:

