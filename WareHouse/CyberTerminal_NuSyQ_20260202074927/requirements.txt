
This file lists the versions of the required packages (`pygame`, `pydantic`, and `pyyaml`) that your project depends on. You can install these dependencies using pip by running the following command in your terminal:

